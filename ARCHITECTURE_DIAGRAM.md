# System Architecture - Betsy AI Excel/CSV Processing

## Pattern Implementation

```
┌─────────────────────────────────────────────────────────────┐
│                    THE CHATGPT PATTERN                       │
│                                                              │
│  Excel/CSV → Pandas/DataFrame (source of truth)             │
│  PDF rules → Vector DB (optional)                           │
│  LLM → reasoning + explanation                              │
│  Python → calculations                                      │
└─────────────────────────────────────────────────────────────┘

## Complete System Flow

┌──────────────┐
│     USER     │  "What are total expenses for Tennessee?"
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│              BETSY AI (Next.js Web App)                      │
│  - Receives user question                                    │
│  - Identifies document type (CSV/PDF)                        │
│  - Routes to appropriate processor                           │
└──────┬───────────────────────────────────────┬──────────────┘
       │                                       │
       │ (Excel/CSV)                           │ (PDF/Text)
       ▼                                       ▼
┌─────────────────────┐              ┌────────────────────┐
│  PYTHON SERVICE     │              │  VECTOR DATABASE   │
│  (PORT 5001)        │              │   (Supabase)       │
│                     │              │                    │
│  1. Load DataFrame  │              │  1. Embed document │
│     df = pd.read()  │              │  2. Store vectors  │
│                     │              │  3. Semantic search│
│  2. Hold in memory  │              │                    │
│     (Source of      │              │  (Optional context)│
│      Truth)         │              │                    │
└──────┬──────────────┘              └─────────┬──────────┘
       │                                       │
       │ DataFrame ready                       │ Context ready
       ▼                                       │
┌─────────────────────────────────────────────┴──────────────┐
│              LLM - GPT-4o (Step 1)                          │
│  → UNDERSTAND: User wants sum of expenses for Tennessee    │
│  → GENERATE: Python/Pandas code                            │
│                                                             │
│     result = df[df['State'] == 'Tennessee']['Amount'].sum()│
└──────┬──────────────────────────────────────────────────────┘
       │
       │ Generated code
       ▼
┌─────────────────────┐
│  PYTHON SERVICE     │  EXECUTE CODE
│  (Calculator)       │  
│                     │  ┌─────────────────┐
│  namespace = {      │  │  CALCULATIONS   │
│    'df': dataframe, │  │  ─────────────  │
│    'pd': pandas,    │──▶│  • Filter rows  │
│    'np': numpy      │  │  • Sum values   │
│  }                  │  │  • Pure math    │
│                     │  │  • No LLM!      │
│  exec(code)         │  └─────────────────┘
│  return result      │
└──────┬──────────────┘
       │
       │ Result: {"type": "number", "value": 210000}
       ▼
┌─────────────────────────────────────────────────────────────┐
│              LLM - GPT-4o (Step 2)                          │
│  → INTERPRET: The calculation returned 210,000             │
│  → EXPLAIN: Format in natural language                     │
│                                                             │
│  "Based on your uploaded file, the total expenses for      │
│   Tennessee are $210,000. This includes all categories     │
│   (Sales, Marketing, and Operations) across both months."  │
└──────┬──────────────────────────────────────────────────────┘
       │
       │ Natural language explanation
       ▼
┌──────────────┐
│     USER     │  Receives clear, ChatGPT-like answer
└──────────────┘

## Component Breakdown

┌─────────────────────────────────────────────────────────────┐
│                    DATABASE = MEMORY                         │
│  ┌────────────────────────────────────────────┐             │
│  │  Pandas DataFrame (In-Memory)              │             │
│  │  ─────────────────────────────             │             │
│  │  • Holds all spreadsheet data              │             │
│  │  • Fast random access                      │             │
│  │  • No database queries needed              │             │
│  └────────────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   PYTHON = CALCULATOR                        │
│  ┌────────────────────────────────────────────┐             │
│  │  Python/Pandas Service                     │             │
│  │  ─────────────────────                     │             │
│  │  • Executes mathematical operations        │             │
│  │  • Handles data transformations            │             │
│  │  • 100% accurate calculations              │             │
│  │  • NO LLM hallucinations                   │             │
│  └────────────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   LLM = INTERPRETER                          │
│  ┌────────────────────────────────────────────┐             │
│  │  GPT-4o (OpenAI)                          │             │
│  │  ─────────────                             │             │
│  │  • Understands natural language            │             │
│  │  • Generates Python code                   │             │
│  │  • Explains results conversationally       │             │
│  │  • Provides context and insights           │             │
│  └────────────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────┘

## Security Layer

┌─────────────────────────────────────────────────────────────┐
│              SECURE CODE EXECUTION                           │
│  ┌────────────────────────────────────────────┐             │
│  │  Forbidden Operations:                     │             │
│  │  • import os                               │             │
│  │  • import sys                              │             │
│  │  • exec(), eval()                          │             │
│  │  • File operations: open()                 │             │
│  │  • Subprocess calls                        │             │
│  │                                            │             │
│  │  Allowed Operations:                       │             │
│  │  • Pandas: df.groupby(), df.sum(), etc.   │             │
│  │  • NumPy: mathematical operations          │             │
│  │  • DataFrame manipulations                 │             │
│  └────────────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────┘

## Why This Pattern Works

┌─────────────┬──────────────────────────────────────────────┐
│  Component  │  Why It Matters                              │
├─────────────┼──────────────────────────────────────────────┤
│  DataFrame  │  • Source of truth (not embeddings)          │
│  (Memory)   │  • Fast access to all data                   │
│             │  • Maintains data integrity                  │
├─────────────┼──────────────────────────────────────────────┤
│  Python     │  • Industry-standard data analysis           │
│  (Calc)     │  • 100% accurate calculations                │
│             │  • No LLM math errors                        │
├─────────────┼──────────────────────────────────────────────┤
│  LLM        │  • Natural language understanding            │
│  (Interp)   │  • Code generation from questions            │
│             │  • Human-friendly explanations               │
└─────────────┴──────────────────────────────────────────────┘

## Real Example

User: "What are total sales by state?"

┌─────────────────────────────────────────────────────────────┐
│ 1. LLM understands question → needs groupby + sum           │
│ 2. LLM generates Python:                                    │
│    result = df[df['Category']=='Sales'].groupby('State')   │
│                                         ['Amount'].sum()    │
│ 3. Python executes → accurate calculation                   │
│ 4. LLM formats results:                                     │
│    "Based on your file, here are total sales by state:     │
│     • California: $257,000                                  │
│     • Texas: $201,000                                       │
│     • Tennessee: $139,000                                   │
│     • New York: $297,000"                                   │
└─────────────────────────────────────────────────────────────┘

## Comparison to Traditional Approach

┌────────────────┬─────────────────┬──────────────────────────┐
│   Approach     │   Traditional   │   Pandas Pattern (Ours)  │
├────────────────┼─────────────────┼──────────────────────────┤
│ Data Storage   │ Embeddings      │ DataFrame (source)       │
│ Calculations   │ LLM (hallucin.) │ Python (accurate)        │
│ Complex Queries│ Limited         │ Full Pandas power        │
│ Accuracy       │ ~80-90%         │ 100%                     │
│ Speed          │ Slow (vectors)  │ Fast (in-memory)         │
└────────────────┴─────────────────┴──────────────────────────┘

This is the CORRECT way to build AI for spreadsheets! 🚀
